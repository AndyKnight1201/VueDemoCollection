{
  "name": "chrome-platform-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "predev": "npm run generate:media",
    "build": "vite build",
    "prebuild": "npm run generate:media",
    "preview": "vite preview",
    "generate:media": "node scripts/generate-wav.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "vue": "3.5.42",
    "vue-router": "5.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.8",
    "vite": "8.2.2"
  }
}
